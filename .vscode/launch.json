{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "unlock-repo",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "envFile": "${workspaceFolder}/.env",
            "args": [
                "--org",
                "mindfulrob",
                "--repo",
                "java-private-library"
            ]
        }
    ]
}